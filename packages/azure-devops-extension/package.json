{
  "name": "@pipelineiq/azure-devops-extension",
  "version": "0.2.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format cjs --no-dts --clean --target node20",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^4.10.0",
    "azure-devops-node-api": "^13.0.0",
    "@pipelineiq/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
